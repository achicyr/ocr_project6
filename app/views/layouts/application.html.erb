<!DOCTYPE html>
<html>
  <head>
    <title>OcrProject6</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body id="<%= "controller_"+controller_name %>" class="<%= "container action_"+params[:action] %>">
    <div id="bo_dashboard" class="row">
      <ul class="todolist todo col clean text-warning">
        <li>mettre en place le lien vers le compte user</li>
      </ul>
      <ul class="todolist done col clean text-success">
        <li>footer: créer des json/hash, pour boucler dessus et générer les infos de "partenars" et "contact"</li>
        <li>les pages sign in, sign up à intégrer avec bootstrap</li>
        <li></li>
      </ul>
    </div>
    <header class="mb-3">
  		<nav class="navbar navbar-expand-lg navbar-light">
  		  <a href="<%= root_path %>" id="logo" class="navbar-brand mr-5">
  		    <span>Move</span>
  		    <span>and</span>
  		    <span>Groove</span>
  		  </a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavAltMarkup">
          <div class="navbar-nav ml-5 ">
		      <%= active_link_to "Your Activities", activities_path, class: "nav-item nav-link btn btn-light" if user_signed_in? %>
		      <%= active_link_to current_user.email[(current_user.email.index('.')+1)..(current_user.email.index('@')-1)], current_user, class: "nav-item nav-link btn btn-light" if user_signed_in? %>
		      <%= active_link_to "Login", new_user_session_path, class: "nav-item nav-link btn btn-light" if !user_signed_in? %>
		      <%= active_link_to "Signup", new_user_registration_path, class: "nav-item nav-link btn btn-light" if !user_signed_in? %>
		      <%= active_link_to "Logout", destroy_user_session_path, method: :delete, class: "nav-item nav-link btn btn-light" if user_signed_in? %>
          </div>
        </div>
      </nav>
      <% if notice %>
        <p id="notice" class="text-center bg-success py-3"><%= notice %></p>
      <% end %>
  	</header>

    <%= render "jumbotron" if current_page? root_path %>
  	<main>
      <%= yield %>
  	</main>
    <%= render "layouts/footer" %>
  </body>
</html>
